name: sandbox
version: 0.2.3
license: MPL2
description: |
  A Crystal shard for running shell commands inside a
  platform-native sandbox, with a configurable access policy

crystal: ">= 1.0.0"

authors:
  - nogginly <nogginly@icloud.com>

development_dependencies:
  ameba:
    github: crystal-ameba/ameba

targets:
  sandboxer:
    main: src/sandboxer_cli.cr
